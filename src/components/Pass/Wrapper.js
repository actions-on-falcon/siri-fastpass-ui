import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  background: white;
  padding: 17px;
  box-shadow: 10px 10px 45px -10px rgba(0, 0, 0, 0.15);
`

export default Wrapper
